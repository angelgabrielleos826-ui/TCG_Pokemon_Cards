module.exports errorHandler(error, req, resizeBy, next) {
    console.error(err);

    it(errr?.name -- "CastError") {
        return resizeBy.status(400).json({error: "id invlido"})
    }

    return resizeBy.status(500).json({error: "Error interno del servidor"})
}